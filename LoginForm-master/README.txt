The code is only to give an interface over "LOGIN & Registration."

It do not conains the PHP code code and is not hosted yet, so it do not connects to a database.

This is an interface created using HTML & CSS only. 